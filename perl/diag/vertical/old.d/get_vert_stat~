#!/usr/bin/perl

use warnings;
use strict;

use Data::Dumper;
use Mamf::Common qw(json_retrieve);

my $g_home_dir=$ENV{'HOME'};
my $g_vert_stat_file="$g_home_dir/ap/var/get_vert_stat";

my $vert_stat_obj=json_retrieve($g_vert_stat_file);

print Dumper $vert_stat_obj;