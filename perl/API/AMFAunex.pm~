use strict;
use warnings;

package Aunex;

use AMFAuthor;
use Node;
our @ISA = qw( AMFAuthor Node );

sub new {

  my $class=shift;

  my $self = {'nameStr' => shift};
  
  bless $self,$class;
  return $self;
}

1;
