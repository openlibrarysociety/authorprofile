#!/usr/bin/perl

use strict;
use warnings;

package Edge;

sub new {

  my $class=shift;

  my $self={'initialNode' => shift,
            'terminalNode' => shift,
            'weight' => shift
           };

  bless $self,$class;
  return $self;
}
